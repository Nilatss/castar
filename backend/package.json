{
  "name": "castar-auth-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secrets": "echo 'Run: wrangler secret put BOT_TOKEN && wrangler secret put JWT_SECRET && wrangler secret put RESEND_API_KEY'"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240129.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.28.0"
  }
}
